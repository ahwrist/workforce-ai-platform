"""Interviewer conversational agent — implemented in Phase 1f."""
import logging

logger = logging.getLogger(__name__)


async def get_interviewer_response(session_token: str, user_message: str):
    """Stream Interviewer agent response for a given user message."""
    # Implemented in Phase 1f
    raise NotImplementedError("Interviewer agent — Phase 1f")
